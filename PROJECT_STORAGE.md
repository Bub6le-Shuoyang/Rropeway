# Rropeway 项目存储规范

Rropeway 项目采用“轻量索引 + 可复用 JSON 组件 + 原始素材文件”的结构。复制整个项目名称文件夹即可完成迁移或备份。

## 目录结构

```text
项目名称/
├─ 项目名称.scriptroom                 # 当前项目索引
├─ 项目名称.scriptroom.backup          # 上一次成功保存的索引
├─ project-data/
│  └─ components/
│     ├─ project/                      # 项目名称、简介和更新时间
│     ├─ characters/                   # 角色资料
│     ├─ items/                        # 物品资料；场景中的物品实例不存这里
│     ├─ assets/                       # 素材库元数据
│     ├─ relationship-graph/           # 角色关系图
│     ├─ scene-flow/                   # 全项目流程图
│     ├─ chapter-index/                # 章节顺序索引
│     ├─ chapters/                     # 每章的元数据和场景引用
│     └─ scenes/                       # 每个场景独立 JSON
└─ assets/
   ├─ images/
   ├─ audio/
   ├─ items/<物品 ID>/images/
   └─ characters/<角色 ID>/
      ├─ avatars/
      └─ portraits/
```

## 索引规则

- `.scriptroom` 是 UTF-8 JSON，只保存格式版本、当前修订号和各组件引用。
- 组件引用由相对路径和 SHA-256 校验值组成，禁止绝对路径和越界路径。
- 每个组件文件包含自身格式、版本和校验值，读取时会验证内容完整性。
- 场景正文只存在于对应场景组件中；角色、物品和素材各自独立，不在场景文件中重复保存。
- 场景中的物品是独立实例，只保存物品 ID、该次调查反应和该次内容流，可以递归包含物品。

## 保存与恢复

1. 将变化的组件写入临时文件并校验。
2. 未变化的组件沿用已有内容哈希文件，不重复写入。
3. 所有组件成功后，原子替换 `.scriptroom` 索引。
4. 替换前的索引保存为 `.backup`。
5. 清理当前索引和备份索引均未引用的旧组件。

因此，修改一个场景时不会复制其他场景、角色、物品或素材数据。应用异常退出也不会留下指向半写入组件的正式索引。

## 兼容策略

- `legacy-json`：早期全部内容存放在一个 `.scriptroom` JSON 中，可直接读取。
- `split-json-v1`：早期按修订目录复制全量组件，可直接读取。
- `split-json-v2`：当前内容寻址组件格式。

旧项目不会在打开时被强制改写；下一次成功保存时自动升级到当前格式。第一次升级会保留旧索引作为备份，第二次成功保存后才清理不再被任何备份引用的旧修订目录。

## 修改边界

- 业务代码只操作规范化后的项目对象，不直接拼接组件路径。
- 文件拆分、校验、兼容读取和原子提交全部由 `project-files.js` 负责。
- 数据字段默认值与旧字段迁移由 `project-format.js` 负责。
- Electron 主进程只通过这两个模块打开或保存项目，渲染进程不直接写磁盘。
